interface:
  display_name: "原型交互生成游戏UI"
  short_description: "把原型和交互流程生成游戏UI提示词"
  default_prompt: "Use $prototype-interaction-game-ui to turn a game UI prototype, interaction flow, or reference image into a polished game UI prompt with a functional breakdown, style extraction table, parameter block, and negative prompt."
policy:
  allow_implicit_invocation: true
